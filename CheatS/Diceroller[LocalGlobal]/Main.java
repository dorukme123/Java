package mypackage;

public class Main {
    public static void main(String[] args) {

        // Local : declared inside the method.
        //         visible only to the method.
        // Global : declared outside the method.
        //          visible to everyone inside the class.

        DiceRoller diceRoller = new DiceRoller();
    }
}